# THIS CODE IS DEPRECATED AND UNSUPPORTED

It was created when the Node core team refused to accept a patch to the language that added this functionality but after enough others complained this functionality was added around the 0.9 release. You should likely be using it instead

Node bindings for initgroups(3) and getgroups(3)

